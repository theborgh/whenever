export const isInitiallySelected = (i: number, daysToDisplay: number, rowsToDisplay: number, users: any) => {
// check users.availability.timeRanges[].startTime and .endTime

// for now, just check the current user (for the interactive part)
// in the future, add a different class for each user, they will have their own color

  return false;
}